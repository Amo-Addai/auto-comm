
export const init = startup => (
    // todo: work with startup
    null
)

export default (exec) => // runtime exec, dynamically injected as dependency
    async () => ( // cloud function signature
    null
)
