
// auto dependency-injection of 'non - env / sensitive' data into config
// * to ensure no Inversion of Control in this case

export default {
    
    injectEnv: () => { config: null },

    //
    
}
